{
  "name": "@kb-labs/cli",
  "version": "1.0.0",
  "description": "KB Labs CLI tool for project management and automation",
  "type": "module",
  "author": {
    "name": "Kirill Baranov",
    "url": "https://github.com/kirill-baranov"
  },
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/kirill-baranov/kb-labs-cli.git"
  },
  "bugs": {
    "url": "https://github.com/kirill-baranov/kb-labs-cli/issues"
  },
  "homepage": "https://github.com/kirill-baranov/kb-labs-cli#readme",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "// ——— devkit": "────────────────────────────────────────",
    "devkit:sync": "node scripts/devkit-sync.mjs",
    "predevkit:sync": "pnpm devkit:paths",
    "devkit:paths": "pnpm exec kb-devkit-paths",
    "devkit:sync:ci": "node scripts/devkit-sync.mjs --ci-only",
    "predevkit:sync:ci": "pnpm devkit:paths",
    "devkit:check": "node scripts/devkit-sync.mjs --check",
    "predevkit:check": "pnpm devkit:paths",
    "devkit:force": "node scripts/devkit-sync.mjs --force",
    "predevkit:force": "pnpm devkit:paths",
    "devkit:help": "node scripts/devkit-sync.mjs --help",
    "postinstall": "pnpm -s devkit:sync || true",
    "// ——— core": "────────────────────────────────────────",
    "clean": "rimraf dist",
    "build": "pnpm run build:commands && pnpm run build:bin",
    "build:commands": "pnpm -C packages/cli-commands run build",
    "build:bin": "pnpm -C packages/cli-bin run build",
    "build:clean": "pnpm -C packages/cli-commands run clean && pnpm -C packages/cli-commands run build && pnpm -C packages/cli-bin run clean && pnpm -C packages/cli-bin run build",
    "dev": "pnpm -r --parallel --filter @kb-labs/cli-* run dev",
    "// ——— quality": "────────────────────────────────────────",
    "lint": "pnpm -r --filter './packages/*' run lint",
    "lint:fix": "pnpm -r --filter './packages/*' run lint:fix",
    "format": "prettier --write .",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "type-check": "pnpm -r --filter './packages/*' run type-check",
    "// ——— ci/qa": "────────────────────────────────────────",
    "check": "pnpm lint && pnpm type-check && pnpm test -- --run",
    "ci": "pnpm clean && pnpm build && pnpm check",
    "// ——— cleanup": "────────────────────────────────────────",
    "clean:all": "rimraf node_modules packages/*/node_modules apps/*/node_modules dist",
    "// ——— cli": "────────────────────────────────────────",
    "kb": "node packages/cli-bin/dist/bin.js"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md,yml,yaml}": [
      "prettier -w"
    ],
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ]
  },
  "dependencies": {
    "@kb-labs/core": "^1.0.0",
    "@kb-labs/devkit": "link:../kb-labs-devkit",
    "@kb-labs/devlink-core": "link:../kb-labs-devlink/packages/devlink-core",
    "@kb-labs/mind-cli": "link:../kb-labs-mind/packages/mind-cli",
    "@kb-labs/mind-core": "link:../kb-labs-mind/packages/mind-core",
    "@kb-labs/mind-indexer": "link:../kb-labs-mind/packages/mind-indexer",
    "@kb-labs/mind-query": "link:../kb-labs-mind/packages/mind-query",
    "@kb-labs/mind-types": "link:../kb-labs-mind/packages/mind-types",
    "@kb-labs/core-cli": "link:../kb-labs-core/packages/core-cli",
    "@kb-labs/release-manager-cli": "link:../kb-labs-release-manager/packages/release-manager-cli",
    "@kb-labs/release-manager-core": "link:../kb-labs-release-manager/packages/release-manager-core",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@actions/core": "^1",
    "@actions/github": "^6",
    "@types/node": "^24.3.3",
    "@types/picomatch": "^4",
    "@vitest/coverage-v8": "^3.2.4",
    "better-sqlite3": "^12.2.0",
    "colorette": "^2.0.20",
    "prettier": "^3",
    "rimraf": "^6",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^3.2.4",
    "@kb-labs/devkit": "workspace:*"
  },
  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.11.0"
}
